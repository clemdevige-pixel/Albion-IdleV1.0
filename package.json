{
  "name": "albion-idle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Albion Idle — monorepo (Phase 01: Repository Bootstrap).",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "build": "tsc -b && pnpm --filter @game/client build",
    "typecheck": "tsc -b && pnpm --filter @game/client typecheck",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate:data": "tsx packages/tooling/src/bin/validate-data.ts",
    "validate:assets": "tsx packages/tooling/src/bin/validate-assets.ts",
    "benchmark:weapon-roles": "tsx --tsconfig tsconfig.base.json scripts/runtime-weapon-role-world-sweep.ts",
    "benchmark:boss-roles": "tsx --tsconfig tsconfig.base.json scripts/runtime-boss-role-telemetry.ts",
    "benchmark:boss-only": "tsx --tsconfig tsconfig.base.json scripts/runtime-boss-only-role-telemetry.ts",
    "benchmark:blue-contract": "tsx --tsconfig tsconfig.base.json scripts/runtime-blue-progression-contract-audit.ts",
    "benchmark:global-enchantment-walls": "tsx --tsconfig tsconfig.base.json scripts/runtime-global-enchantment-wall-matrix.ts",
    "benchmark:enchantment-walls": "tsx --tsconfig tsconfig.base.json scripts/runtime-enchantment-progression-audit.ts",
    "benchmark:final-gates": "tsx --tsconfig tsconfig.base.json scripts/runtime-final-gate-audit.ts",
    "benchmark:tier-transitions": "tsx --tsconfig tsconfig.base.json scripts/runtime-tier-plateau-audit.ts",
    "benchmark:enchantment-shards": "tsc -b && tsx --tsconfig tsconfig.base.json scripts/runtime-enchantment-shard-progression.ts",
    "benchmark:global-balance": "pnpm typecheck && pnpm validate:data && pnpm benchmark:weapon-roles && pnpm benchmark:boss-only && pnpm benchmark:blue-contract && pnpm benchmark:global-enchantment-walls && pnpm benchmark:enchantment-walls && pnpm benchmark:final-gates && pnpm benchmark:tier-transitions && pnpm benchmark:enchantment-shards && pnpm --filter @game/client test -- src/data/globalWeaponZoneClearBenchmark.test.ts src/data/itemPowerCrossSpecialization.test.ts src/data/factionArtifactWeaponBenchmark.test.ts src/data/factionArtifactDungeonBonusABBenchmark.test.ts src/data/factionArtifactDungeonFavorableClearBenchmark.test.ts src/data/factionArtifactDungeonT5ToT8FavorableClearBenchmark.test.ts src/data/factionArtifactDungeonT5ToT8LeakBenchmark.test.ts src/data/dungeonT4BaseWeaponProgressionBenchmark.test.ts src/data/artifactWeaponCraftRecipes.test.ts src/data/artifactWeaponEnchantmentContract.test.ts src/data/awakenedDefensiveTraitCalibrationAudit.test.ts src/data/globalEconomySequentialSetChain.test.ts",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.19.1",
    "vitest": "^2.1.8"
  }
}
