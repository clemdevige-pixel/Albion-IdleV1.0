{
  "name": "@game/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic simulation engine. Runtime Core (02.1) and Entity Framework (02.2). No gameplay, no React/Phaser/Fastify/Node coupling.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean && tsc -b tsconfig.build.json --clean"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
